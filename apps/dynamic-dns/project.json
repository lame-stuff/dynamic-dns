{
  "root": "apps/dynamic-dns",
  "sourceRoot": "apps/dynamic-dns/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/node:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/dynamic-dns",
        "main": "apps/dynamic-dns/src/main.ts",
        "tsConfig": "apps/dynamic-dns/tsconfig.app.json",
        "assets": ["apps/dynamic-dns/src/assets"]
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false,
          "fileReplacements": [
            {
              "replace": "apps/dynamic-dns/src/environments/environment.ts",
              "with": "apps/dynamic-dns/src/environments/environment.prod.ts"
            }
          ]
        }
      }
    },
    "serve": {
      "executor": "@nrwl/node:execute",
      "options": {
        "buildTarget": "dynamic-dns:build"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/dynamic-dns/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/dynamic-dns"],
      "options": {
        "jestConfig": "apps/dynamic-dns/jest.config.js",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
